<% provide :title, @user.name %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user, size: 50 , html_class: 'gravatar'%>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @articles.any? %>
    <h3>
      <%= @user.articles.count %>
      <%= 'article'.pluralize(@user.articles.count) %>
      publiés
    </h3>
    <ol class="articles">
      <%= render @articles %>
    </ol>
    <%= will_paginate @articles %>
    <% end %>
  </div>
</div>
